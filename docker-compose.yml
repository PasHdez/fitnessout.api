version: "3.8"
services:
  postgres:
    image: postgres:10.3
    restart: always
    environment:
      - POSTGRES_PASSWORD=fit4less123
      - POSTGRES_USER=fitnessout
      - POSTGRES_DB=fitnessout
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432
volumes:
  postgres:
